# Local development uses the NestJS server directly. For the current separate
# production API, set this in Vercel to:
# https://chatapp.vorionsystems.com/api
# Do not use a frontend URL or an endpoint such as /auth/login here.
NEXT_PUBLIC_API_BASE=http://localhost:3010/api
